# PWA Icon Placeholder

The `icon.svg` file is a placeholder icon. Replace it with your actual app icon and generate the required PNG sizes using the instructions in `PWA-ICONS-SETUP.md`.

To generate all required icon sizes from the SVG:

```bash
# Using ImageMagick or another tool, convert the SVG to the required PNG sizes
# See PWA-ICONS-SETUP.md for detailed instructions
```
